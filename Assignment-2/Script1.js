function checkRoomAvailability() {
    // Sample room availability status
    var isRoomAvailable = false;

    // Check if the room is available
    if (!isRoomAvailable) {
        // Alert message if the room is not available
        alert("Sorry, the room is not available.");
    }
}